// User-facing app version (semver). Bump on each meaningful release:
//   MAJOR.MINOR.PATCH — MINOR for features, PATCH for fixes.
// Kept separate from app.json "version" (which stays 1.0.0 as the native
// runtimeVersion so over-the-air updates keep reaching installed builds).
export const APP_VERSION = "1.25.0";
