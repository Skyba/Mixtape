rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Each user can only read/write their own namespace: recordings/{their uid}/...
    match /recordings/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
